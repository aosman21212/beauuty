<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- List -->
    <record id="view_spa_package_list" model="ir.ui.view">
        <field name="name">spa.package.list</field>
        <field name="model">spa.package</field>
        <field name="arch" type="xml">
            <list string="Packages">
                <field name="name"/>
                <field name="total_duration" string="Duration (min)"/>
                <field name="list_price" string="Normal Price" widget="monetary"
                       options="{'currency_field': 'currency_id'}"/>
                <field name="package_price" widget="monetary"
                       options="{'currency_field': 'currency_id'}"/>
                <field name="savings" widget="monetary"
                       options="{'currency_field': 'currency_id'}"/>
                <field name="currency_id" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Form -->
    <record id="view_spa_package_form" model="ir.ui.view">
        <field name="name">spa.package.form</field>
        <field name="model">spa.package</field>
        <field name="arch" type="xml">
            <form string="Treatment Package">
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="Package name…"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="package_price" widget="monetary"
                                   options="{'currency_field': 'currency_id'}"/>
                            <field name="currency_id"/>
                            <field name="total_duration" readonly="1"/>
                        </group>
                        <group>
                            <field name="list_price" readonly="1" widget="monetary"
                                   options="{'currency_field': 'currency_id'}"/>
                            <field name="savings" readonly="1" widget="monetary"
                                   options="{'currency_field': 'currency_id'}"/>
                            <field name="active"/>
                        </group>
                    </group>
                    <field name="line_ids">
                        <list editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="service_id"/>
                            <field name="quantity"/>
                            <field name="subtotal" widget="monetary"
                                   options="{'currency_field': 'currency_id'}"/>
                            <field name="currency_id" column_invisible="1"/>
                        </list>
                    </field>
                    <field name="description" widget="html" placeholder="Package description…"/>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_spa_package" model="ir.actions.act_window">
        <field name="name">Packages</field>
        <field name="res_model">spa.package</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
